ex = [8,4,3,2,5]

for k in range(0,len(ex)-1):
    i = 0
    j = i + 1
    a = ex[i]
    b = ex[j]
    while a > b:
        (ex[i], ex[j]) = (ex[j], ex[i])
        (i, j) = (i+1, j+1)
        if j == len(ex):
            break
        a = ex[i]
        b = ex[j]
        print(ex)
